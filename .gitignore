# Secrets / credentials
.env
auth/
.claude/
# Real handles file holds live Postiz ids — commit handles.example.yaml instead
products/arboryx.ai/handles.yaml

# Logs
*.log
dev-utils/error.log

# Local dev utilities — rotation scripts (touch live creds) + history backups
dev-utils/rotate-db-passwords.sh
dev-utils/*.bundle

# Local scratch notes (session ids, OAuth probe URLs, etc.)
notes.txt

# Internal engineering log — kept local, not published
docs/workbench.md

# Local data — posted-log SQLite + generated imagery cache (per-machine, not shared)
data/posted_log.sqlite
data/posted_log.sqlite-journal
data/imagery_cache/
data/content_cache/
data/manual-post-queue.md
data/linkedin_urn_cache.json

# Python
__pycache__/
*.pyc
