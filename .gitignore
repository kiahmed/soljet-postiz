# Secrets / credentials
.env
auth/
.claude/
# Real handles files hold live Postiz ids — commit the *.example.yaml instead
products/arboryx.ai/handles.yaml
products/facades/facades_handles.yaml

# Logs
*.log
dev-utils/error.log

# Local dev utilities — rotation scripts (touch live creds) + history backups
dev-utils/rotate-db-passwords.sh
dev-utils/*.bundle

# Local scratch notes (session ids, OAuth probe URLs, etc.)
notes.txt

# Internal engineering log — kept local, not published
docs/workbench.md

# Local data — posted-log SQLite + generated imagery cache (per-machine, not shared)
data/posted_log.sqlite
data/posted_log.sqlite-journal
data/*.bak*
data/imagery_cache/
data/content_cache/
data/manual-post-queue.md
data/linkedin_urn_cache.json
data/render_gcs_cache.json
# bin/simmer_poster.py local-mode dedupe (per-machine)
data/simmer_poster_dedupe.json

# Postiz DB/media export for machine migration — contains LIVE OAuth tokens
# and the Postiz login hash. Move by hand (scp/USB); never commit.
data/postiz_export/

# Python
__pycache__/
*.pyc
.venv/

# Generated scheduler crontab (source: ops/scheduler/channels.conf)
ops/scheduler/crontab
