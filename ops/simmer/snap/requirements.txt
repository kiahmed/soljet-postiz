flask>=3.0.0
gunicorn>=22.0.0
# Must match the base image's bundled browsers (mcr.microsoft.com/playwright/python
# :v1.47.0-jammy ships the Chromium binaries but NOT the pip module).
playwright==1.47.0
